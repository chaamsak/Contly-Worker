generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model VideoJob {
  id        String      @id
  status    VideoStatus @default(PENDING)
  videoUrl  String?
  error     String?
}

enum VideoStatus {
  PENDING
  PROCESSING
  READY
  FAILED
}
